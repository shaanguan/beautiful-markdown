/** Entry for the vendor IIFE — only what Paper preset needs. */
export {
  ShaderMount,
  paperTextureFragmentShader,
  getShaderColorFromString,
  getShaderNoiseTexture,
  ShaderFitOptions,
  emptyPixel
} from "@paper-design/shaders";
