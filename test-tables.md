# 表格列宽测试

用 Beautiful Markdown 打开本文件，检查短字段列是否保持单行、长文本列是否吃掉剩余宽度、宽表是否横向滚动。

## 1. 少列：短字段 + 长说明（核心场景）

| 状态 | 版本 | 说明 |
|------|------|------|
| 完成 | 1.7.2 | 渲染本地 Markdown，支持 KaTeX、Mermaid、预设主题与阅读宽度切换。 |
| 进行中 | 1.8.0 | 表格列宽按内容分流：短列 nowrap，长列自适应，多列溢出时外层横滑。 |
| 计划 | — | 选区菜单、目录刻度提示与阅读主题保持一致，避免系统深色与页面浅色错位。 |

**预期：**「状态」「版本」单行且偏窄；「说明」占满剩余宽度并正常换行。

## 2. 数字 / 尺寸 / 日期短列

| # | 尺寸 | 更新 | 备注 |
|---|------|------|------|
| 1 | 48px | 2026-08-07 | 图标资源，用于扩展工具栏与首页品牌区。 |
| 2 | 128px | 2026-08-01 | 商店宣传图，要求透明背景、边缘干净。 |
| 12 | 16px | 2026-07-22 | 小尺寸 favicon，深浅色下都要可读。 |

**预期：** `#`、`尺寸`、`更新` 不折行；`备注` 为长文本列。

## 3. 两列对照（短 | 长）

| 项 | 内容 |
|----|------|
| 预设 | default / claude / minimal / stone-gnome |
| 模式 | auto 跟随系统；light / dark 记住上次选择 |
| 宽度 | standard / wide / full / split（阅读页） |

**预期：** 左列紧凑，右列舒展。

## 4. 对齐列（GFM）

| 左对齐 | 居中 | 右对齐 | 说明 |
|:-------|:----:|-------:|------|
| name | ok | 12 | 左文右数是常见数据表形态。 |
| path | fail | 1024 | 数字列右对齐后视觉上应更窄、更整齐。 |
| hash | — | 3 | 短状态居中，长说明仍应换行。 |

## 5. 多列宽表（应出现横滑）

| A | B | C | D | E | F | G | H |
|---|---|---|---|---|---|---|---|
| alpha | bravo | charlie | delta | echo | foxtrot | golf | hotel |
| 项目名称较长时 | 负责人 | 优先级 | 截止日期 | 环境 | 分支 | 评审人 | 备注列也可能很长以至于整表超出阅读栏 |

**预期：** 外层 `.bsw-table-wrap` 出现横向滚动，页面本身不横向撑开。

## 6. 含代码与 URL 的单元格

| 类型 | 值 | 说明 |
|------|-----|------|
| 路径 | `src/renderer.js` | 表格渲染后调用 `markTightColumns`。 |
| URL | https://github.com/shaanguan/beautiful-markdown | 长 token 可断词或触发横滑，不应撑破页面。 |
| 命令 | `npm run build` | 短代码列尽量单行。 |

## 7. 几乎全是短列

| ID | 开 | 关 | 级别 |
|----|----|----|------|
| a1 | ✓ | — | P0 |
| b2 | — | ✓ | P1 |
| c3 | ✓ | ✓ | P2 |

**预期：** 各列都紧凑；表宽可小于阅读栏，不必故意撑满留白夸张。

## 8. 几乎全是长文本

| 问题 | 影响 |
|------|------|
| 本地 file:// 下相对路径图片曾被 DOMPurify 剥掉 src，导致有时看不到图。 | 用户以为 Markdown 不支持图片；https 图正常、本地图失败，表现像偶发 bug。 |
| 选区菜单曾跟随系统色，阅读页跟随扩展主题，二者不一致。 | 浅色文档上出现深色悬浮条，或反过来，观感割裂。 |

**预期：** 两列都按段落换行，不要 nowrap 把整表拉成超宽横滑（除非单元格内有极长不可断 token）。

## 9. 混排：表格前后正文

上面策略落地后，短列应稳定单行。下面再跟一段正文，确认表格左右没有把阅读栏撑歪。

> 一句话验收：少列跟内容自适应，多列承认横滑；短字段不碎行，长说明能换行。

---

**快速对照**

1. §1–§3：短列窄、长列宽  
2. §5：横滑  
3. §7：全短列不夸张留白  
4. §8：全长列可换行、少横滑  
